#include "main.h"


/**
* _abs - computes the absolute value of integer
* @c : character to be computed
* Return: Absolute Value
*/

int _abs(int c)
{
		if (c >= 0)
		return (c);
	else
		return (c * -1);
}

